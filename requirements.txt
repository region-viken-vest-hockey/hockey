# Direct runtime dependency declarations for humans/lock refresh.
# Deterministic installs must use requirements.lock instead.
requests>=2.31.0
beautifulsoup4>=4.12.0
openpyxl>=3.1.0
holidays>=0.38
playwright>=1.40.0
icalendar>=6.0.0
recurring-ical-events>=3.0.0
rich>=14.0.0

# Optional: enables loading YAML config files (parallel-games config and
# roster config) via ParallelGamesConfig/RosterLoader in
# tournament_scheduler/season_config.py and tournament_scheduler/roster_loader.py.
# Without it, only JSON config files can be loaded (a clear Norwegian-language
# error is raised if a .yaml/.yml file is requested).
# pyyaml>=6.0

# Optional: enables the shadow/experimental CP-SAT Stage 3 engine
# (tournament_scheduler/stage3_cpsat.py, issue #276), selected via
# `--engine cp-sat`. Without it, engine="cp_sat" raises a clean
# CpSatUnavailable CLI error; engine="local_search" (default) is unaffected.
# ortools>=9.10
